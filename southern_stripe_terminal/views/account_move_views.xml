<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_stripe_terminal" model="ir.ui.view">
        <field name="name">account.move.form.stripe.terminal</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@id='account_invoice_payment_btn']" position="attributes">
                <attribute name="invisible">(
                    move_type == 'out_invoice'
                    or state != 'posted'
                    or payment_state not in ('not_paid', 'partial', 'in_payment')
                    or move_type not in ('out_invoice', 'out_refund', 'in_invoice', 'in_refund', 'out_receipt', 'in_receipt')
                    or invoice_has_outstanding
                )</attribute>
            </xpath>
            <xpath expr="//button[@id='account_invoice_payment_secondary_btn']" position="attributes">
                <attribute name="invisible">(
                    move_type == 'out_invoice'
                    or state != 'posted'
                    or payment_state not in ('not_paid', 'partial', 'in_payment')
                    or move_type not in ('out_invoice', 'out_refund', 'in_invoice', 'in_refund', 'out_receipt', 'in_receipt')
                    or not invoice_has_outstanding
                )</attribute>
            </xpath>
            <xpath expr="//header" position="inside">
                <button name="action_pay_with_stripe_terminal"
                        string="Pay with Terminal"
                        type="object"
                        class="oe_highlight"
                        groups="account.group_account_invoice"
                        invisible="move_type != 'out_invoice' or state != 'posted' or payment_state in ('paid', 'reversed') or amount_residual &lt;= 0"/>
                <button name="action_pay_by_phone"
                        string="Pay by Phone"
                        type="object"
                        class="btn-secondary"
                        groups="southern_stripe_terminal.group_stripe_terminal_moto"
                        invisible="move_type != 'out_invoice' or state != 'posted' or payment_state in ('paid', 'reversed') or amount_residual &lt;= 0"/>
                <button name="action_pay_with_cash"
                        string="Pay with Cash"
                        type="object"
                        class="btn-secondary"
                        groups="account.group_account_invoice"
                        invisible="move_type != 'out_invoice' or state != 'posted' or payment_state in ('paid', 'reversed') or amount_residual &lt;= 0"/>
                <button name="action_pay_with_ach"
                        string="Pay with ACH"
                        type="object"
                        class="btn-secondary"
                        groups="account.group_account_invoice"
                        invisible="move_type != 'out_invoice' or state != 'posted' or payment_state in ('paid', 'reversed') or amount_residual &lt;= 0"/>
            </xpath>
            <xpath expr="//field[@name='invoice_payment_term_id']" position="after">
                <field name="southern_payment_type"
                       readonly="state != 'draft'"
                       invisible="move_type != 'out_invoice'"/>
                <field name="southern_processing_fee_amount"
                       readonly="1"
                       invisible="move_type != 'out_invoice' or southern_processing_fee_amount == 0"/>
            </xpath>
        </field>
    </record>
</odoo>
